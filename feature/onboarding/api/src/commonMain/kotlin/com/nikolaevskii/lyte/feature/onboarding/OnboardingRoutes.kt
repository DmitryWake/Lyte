package com.nikolaevskii.lyte.feature.onboarding

import kotlinx.serialization.Serializable

/**
 * Обучение целиком — один маршрут: и приветствие, и все шаги тура. Шаг живёт полем состояния, а не
 * отдельным маршрутом. Пять маршрутов означали бы пять ViewModel, запись флага «обучение пройдено» в
 * пяти терминальных точках и невозможность анимировать переход подсветки между шагами.
 *
 * Маршрут верхнего уровня, брат `SplashRoute`: он не входит ни в один граф вкладки, поэтому
 * плавающий док скрывается сам, без правок оболочки.
 */
@Serializable
data object OnboardingRoute
